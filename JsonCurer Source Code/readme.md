## Back-end

`cd backend`
`pip install -r requirements.txt`
`python src/app.py`

## Front-end

`cd frontend`
`npm install`
`npm run serve`

Now the project is running on your [localhost](http://localhost:8080/).